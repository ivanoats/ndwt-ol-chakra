node_modules
.DS_Store
dist
dist-ssr
*.local
coverage
.env
build
# Local Netlify folder
.netlify

# Next.js
/.next/
/out/
*.tsbuildinfo
next-env.d.ts

# PandaCSS codegen output (regenerated on every build)
/styled-system/

# Claude Code local state
.claude/
.agents/
skills-lock.json

# Playwright
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# Photo-candidate scraper output — regenerated by
# `python3 scripts/find-photos.py`. Not committed yet because the
# filters are still being iterated; commit a final curated copy
# under a different name once we settle on what to keep.
/public/data/photo-candidates.json
__pycache__/
